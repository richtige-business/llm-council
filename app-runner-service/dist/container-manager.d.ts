export interface ContainerSession {
    sessionId: string;
    containerId: string;
    containerName?: string;
    appName: string;
    targetUrl: string;
    vncPort?: number;
    wsPort?: number;
    viewerPort: number;
    tcpMuxPort: number;
    udpMuxPort: number;
    status: 'starting' | 'running' | 'stopped' | 'error';
    startedAt: number;
    lastError: string | null;
    resolution: string;
    novncUrl?: string;
    viewerUrl?: string;
    internalViewerBaseUrl?: string;
    seleniumUrl?: string;
    userName: string;
    userPassword: string;
    adminPassword: string;
}
declare class ContainerManager {
    private readonly sessionContainerPrefix;
    private readonly dockerHost;
    private readonly dockerNetwork;
    private readonly runtimePath;
    private readonly runtimeVolumeName;
    private readonly childRuntimePath;
    private readonly publicBaseUrl;
    private readonly viewerHost;
    private readonly publicIp;
    private sessions;
    private readonly imageName;
    private nextViewerPort;
    private nextTcpMuxPort;
    private nextUdpMuxPort;
    private readonly maxPortOffset;
    private dockerAvailable;
    constructor();
    private checkDocker;
    ensureImage(): Promise<boolean>;
    startContainer(sessionId: string, appName: string, url?: string, resolution?: string): Promise<ContainerSession>;
    stopContainer(sessionId: string): Promise<void>;
    getSession(sessionId: string): ContainerSession | null;
    getAllSessions(): ContainerSession[];
    getViewerProxyTarget(sessionId: string): string | null;
    stopAll(): Promise<void>;
    isDockerAvailable(): boolean;
    getAppUrl(appName: string): string | null;
    getAvailableApps(): {
        name: string;
        url: string;
    }[];
    private prepareRuntimeFiles;
    private cleanupRuntimeFiles;
    private waitForReady;
    private cleanupOldContainers;
    private getNextPort;
    private buildChromiumSupervisorConfig;
    private buildDirectViewerUrl;
    private buildViewerQuery;
    private buildInternalViewerBaseUrl;
    private parseResolution;
    private createSecret;
    private derivePublicIpFromBaseUrl;
    private escapeShellArg;
}
export declare const containerManager: ContainerManager;
export {};
